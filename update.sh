#!/usr/bin/env bash

##########################################################################
# UPDATE
# Updates repository based on current computer settings.
##########################################################################

cp "$HOME/.bash_profile" ./
cp "$HOME/.bashrc" ./
cp "$HOME/.profile" ./